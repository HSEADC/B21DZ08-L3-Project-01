<%= form_with(model: fandom) do |form| %>
  <% if fandom.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fandom.errors.count, "error") %> prohibited this fandom from being saved:</h2>

      <ul>
        <% fandom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.number_field :author_id %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.number_field :category_id %>
  </div>

  <div>
    <%= form.label :character_id, style: "display: block" %>
    <%= form.number_field :character_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
